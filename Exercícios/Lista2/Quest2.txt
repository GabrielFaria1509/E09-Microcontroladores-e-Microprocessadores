Código original : 
uint8_t buttons = 0;
DDRB |= (1<<PB0) + (1<<PB1) + (1<<PB2); 
DDRD &= ~((1<<PD0) + (1<<PD1) + (1<<PD2)); 
 while (1) 
 { 
   buttons = PIND; 
   switch(buttons) 
 { 
   case 0: 
     PORTB &= ~ (255); 
     break; 
   case 1: 
     PORTB |= 1; 
     break; 
   case 2: 
     PORTB |= 2; 
     break; 
  case 3: 
     PORTB |= 3; 
     break; 
   } 
 }


Código otimizado : 

DDRB |= (1<<PB0) + (1<<PB1) + (1<<PB2); 
DDRD &= ~((1<<PD0) + (1<<PD1) + (1<<PD2));

while(1){
  
}
